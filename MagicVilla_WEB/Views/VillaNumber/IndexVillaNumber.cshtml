@model IEnumerable<MagicVilla_WEB.Models.VillaNumber.Dto.VillaNumberDTO>
<br />
<div class="container p-4 border">
    <div class="row pb-2">
        <div class="col">
            <h1 class="text-primary">Villa Number List</h1>
        </div>
        <div class="col text-end pt-1">
            <a asp-controller="VillaNumber" asp-action="CreateVilla" class="btn btn-outline-primary"><i class="bio bi-plus-square-fill"></i>Create new Villa</a>
        </div>
    </div>


<table class="table">
    <thead>
        <tr>
            <th>
                Villa Name
            </th>
            <th>
                Villa Number
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Villa.Name</td>
                <td>@item.No</td>
                <td>
                    <a asp-controller="Villa" asp-action="UpdateVillaNumber" asp-route-villaId="@item.No" class="btn btn-success">Update</a>
                    <a asp-controller="Villa" asp-action="DeleteVillaNumber" asp-route-villaId="@item.No" class="btn btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
</div>